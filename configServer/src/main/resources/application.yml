server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lauraveronic01/esp-back-i-parcial
          default-label: develop
          force-pull: true
          search-paths: configData
